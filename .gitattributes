# Assets pushed into the Linux guest must keep LF endings. A Windows checkout
# (core.autocrlf=true) rewrites them to CRLF, and bash then reads line 2 of
# bootstrap.sh as `set -euo pipefail\r` and aborts with "invalid option name".
*.sh text eol=lf
omelet/guest/** text eol=lf
omelet/templates/** text eol=lf
omelet/providers/omelet.yaml text eol=lf
