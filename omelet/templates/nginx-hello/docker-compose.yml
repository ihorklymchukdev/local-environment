services:
  web:
    image: nginx:alpine
    # No published host port: traffic reaches this through Traefik on the edge
    # network. Publishing one only creates collisions inside the VM.
    expose:
      - "80"
